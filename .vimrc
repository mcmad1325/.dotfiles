vim Madeleine McBride
vim "Today we created a vim file and edited the contents and saved it as a text file. This was for CSCE 215. We used many commands like the vim, cp, and pwd commands. I was confused a lot of the time because I wasn't sure what a vim file is or how to use it." 
syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord

